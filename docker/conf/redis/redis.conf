# Minimal redis used only for the Laravel Horizon job queue.
bind 127.0.0.1
port 6379
protected-mode yes
daemonize no
dir /tmp/redis
pidfile /tmp/redis/redis.pid
logfile ""
# The queue is transient: skip persistence. Missed transcriptions are retried lazily on the next ramble fetch.
save ""
appendonly no
